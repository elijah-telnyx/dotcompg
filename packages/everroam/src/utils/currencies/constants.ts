export const SUPPORTED_CURRENCIES = ['USD', 'EUR'] as const;

export const DEFAULT_CURRENCY_CODE = 'USD';

export const DEFAULT_CURRENCY_RATE = '1';

export const DEFAULT_CURRENCY_TABLES_DATA = {
  [DEFAULT_CURRENCY_CODE]: [],
};
